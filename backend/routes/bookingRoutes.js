// backend/routes/bookingRoutes.js

const express = require('express');
const router = express.Router();

// Sample route for booking (replace with actual booking logic)
router.get('/', (req, res) => {
  res.send('Booking route');
});

module.exports = router;
